import {View, Text} from 'react-native';
import React from 'react';

type LocationScreenProps = {
  navigation: any;
};

const Location = ({navigation}: LocationScreenProps) => {
  return (
    <View>
      <Text>Shop</Text>
    </View>
  );
};

export default Location;
